services:
  grub-builder:
    build:
      context: .
      dockerfile: Dockerfile
    platform: linux/amd64
    container_name: grub-builder
    volumes:
      - .:/workspace
    working_dir: /workspace
